# Copy to .env for a project, or run `darkops auth --key ...` to store it once
# for every repository. The environment wins over either file.
TYPESAFE_API_KEY=

# Optional: pin a model instead of the `jev-latest` alias (e.g. jev-1.13.0).
TYPESAFE_DEFAULT_MODEL=
